var patternCount = 0;var swatchCount = 0;var spotCount = 0;var cmykCount = 0;var rgbCount = 0;var grayCount = 0;mySelection = activeDocument.pageItems;for(i=0; i<mySelection.length; ++i) {		if( mySelection[i].filled ) {		switch( mySelection[i].typename ) {			case "PathItem" :				tempSwatchType = mySelection[i].fillColor.typename;			case "TextFrame" :				tempSwatchType = mySelection[i].fillColor.typename;			case "CompoundPath" :			case "GroupItems" :			default :				// Must be a object we don't want to evaluate (or haven't defined an evaluation routine)				continue;				break;		}		switch( tempSwatchType ) {			case "PatternColor" :				patternCount++				// Should also check to find out what colors are used in the pattern				break;			case "SwatchProcess" :				swatchCount++;				if( mySelection[i].fillColor.color.typename == "CMYKColor" ) {					cmykCount++;				}				if( mySelection[i].fillColor.color.typename == "RGBColor" ) {					rgbCount++				}				break;			case "SwatchSpot" :				spotCount++;				break;							case "CMYKColor" :				cmykCount++;				break;			case "RGBColor" :				rgbCount++;				break;			case "GrayColor" :				grayCount++;				break;								}	}}alert( "patternCount: " + patternCount + "\nswatchCount: " + swatchCount + "\nspotCount: " + spotCount + "\ncmykCount: " + cmykCount + "\nrgbCount: " + rgbCount + "\ngrayCount: " + grayCount ); 